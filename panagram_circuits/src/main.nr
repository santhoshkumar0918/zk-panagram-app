fn main(guess_hash: Field, number_hash: pub Field) {
    assert(guess_hash = number_hash);
}

// #[test]
// fn test_main() {
//     main(1, 2);

//     // Uncomment to make test fail
//     // main(1, 1);
// }
